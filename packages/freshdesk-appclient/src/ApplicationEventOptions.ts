/**
 * Represents options for the `ApplicationClientEvent.on` function.
 */
export interface ApplicationEventOptions {
    /**
     * Indicates whether the event should be intercepted.
     */
    intercept: boolean;
}